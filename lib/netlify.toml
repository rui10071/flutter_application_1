[build]
  # Flutterのビルドコマンドと公開ディレクトリを指定
  command = "flutter build web"
  publish = "build/web"

[build.environment]
  # 使用したいFlutterのバージョンを指定
  FLUTTER_VERSION = "3.22.2" 

[functions]
  # Netlify Functionsを使用する場合（なければ不要）
  directory = "functions"